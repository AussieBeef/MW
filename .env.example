# Firebase Realtime Database REST root (no trailing slash).
# This value is exposed to the browser (NEXT_PUBLIC_), so lock the database
# with Firebase Security Rules. Do not put admin/service-account keys here.
NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://YOUR-PROJECT-default-rtdb.firebaseio.com
